apiVersion: v1
kind: Service
metadata:
  name: aws-platform-app-service
spec:
  type: ClusterIP

  selector:
    app: aws-platform-app

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
